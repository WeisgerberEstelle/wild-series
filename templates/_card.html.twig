<div class="container col-lg-4 offset-lg-1">
<h2 class="my-4">En ce moment</h2>
	<div class="card card-cat mb-3 mx-0 moment ">
		<div class="row height-card">
			<div class="col-lg-6 d-flex justify-content-center align-items-center card-content">
				<img class="card-img-top image" src="{{ programs.0.poster }}" alt="Card image cap">
			</div>
			<div class="col-lg-6 text-column">
				<div class="card-body">
					<h3 class="card-title text-center">{{ programs.0.title}}</h3>
					<div class="card-text description d-flex flex-column justify-content-start align-items-center">
						<p>{{ programs.0.summary }}</p>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>
